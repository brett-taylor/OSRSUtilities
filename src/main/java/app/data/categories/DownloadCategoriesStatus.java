package app.data.categories;

/**
 * The enums to store the current state of the category's download.
 * @author Brett Taylor
 */
public enum DownloadCategoriesStatus {
    NOT_DOWNLOADED,
    QUEUED,
    DOWNLOADING,
    DOWNLOADED
}
