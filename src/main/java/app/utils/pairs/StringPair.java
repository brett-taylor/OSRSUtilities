package app.utils.pairs;

/**
 * A pair where both types are String.
 * @author Brett Taylor
 */
public class StringPair extends SamePair<String> {
    /**
     * Constructor.
     * @param one variable one
     * @param two variable two
     */
    public StringPair(String one, String two) {
        super(one, two);
    }
}
